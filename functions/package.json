{
  "name": "sitecore-silver-functions",
  "description": "Firebase Cloud Functions for Sitecore Silver Photo Booth",
  "type": "module",
  "main": "lib/index.js",
  "scripts": {
    "lint": "eslint . --ext .ts",
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0",
    "@google/generative-ai": "^0.3.0",
    "sharp": "^0.33.5",
    "axios": "^1.6.0",
    "@sendgrid/mail": "^8.1.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0",
    "eslint": "^8.54.0"
  },
  "engines": {
    "node": "20"
  }
}
